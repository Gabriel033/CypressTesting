describe('JQuery and Cypress', function (){

    const element = $('css-selector')

    if(element.length){
        element.click();
    }

    $.each[1,2,3],(arrayIndex, arrayValue)=>{
        expect(arrayValue).to.eq(arrayValue)
    }

})